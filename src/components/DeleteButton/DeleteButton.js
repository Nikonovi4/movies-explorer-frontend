const DeleteButton = () => {
  return (
    <button
      aria-label="кнопка нравится."
      className="deleteButton"
      type="button"
      //onClick={}
    />
  );
};

export default DeleteButton;
